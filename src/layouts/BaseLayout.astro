---
import EditorialLayout from '@/layouts/EditorialLayout.astro'

interface Props {
  width?: 'prose' | 'wide' | 'wider' | 'full'
  [key: string]: unknown
}
const { width, ...rest } = Astro.props
---

<EditorialLayout mainWidth={width} {...rest}>
  <slot />
</EditorialLayout>
